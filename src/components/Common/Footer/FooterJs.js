import {
    user as UserApi,
    system as SystemApi
} from '../../../config/request.js';

export default {
    name: 'head-nav',
    data() {
        return {
        }
    },
    mounted() {
    },
    methods: {
    }
}